# Green Liquid web app

```bash
cd web
npm install
npm run dev
```

The scored dataset lives at `public/data/eseg_master.json`. Rebuild it from the repo root with:

```bash
python3 scripts/build_eseg_dataset.py
```
